[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "labjack-alicat-control"
version = "0.1.0"
description = "Simple control for a LabJack U3 and Alicat mass flow controller"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "alicat>=0.9,<1",
    "LabJackPython>=2.1,<3",
]

[project.scripts]
lab-control = "lab_control.cli:main"

[dependency-groups]
dev = ["ruff>=0.15,<1"]

[tool.hatch.build.targets.wheel]
packages = ["src/lab_control"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "RUF", "SIM", "UP"]

